# Exploratory map

Useful later work that is out of scope until Stage A residuals demand it.

- Persistent coding attacks across a real repository checkout
- Adaptive attack search with a declared budget
- Open-weight task adaptation and loss-mask verification
- Persona-vector diagnostics compared with behavior-only predictors
- Cross-family teacher/student synthetic-data transfer
- Zero-seed long-horizon runs
- Extracting `permits.py` into a shared `trusted-runtime-lite` package with the control repo
